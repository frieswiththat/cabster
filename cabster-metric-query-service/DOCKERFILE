FROM python:3.8-alpine

WORKDIR /app

COPY ./src /app

RUN pip install -r requirements.txt

ARG DATABASE_SECRET
ENV DATABASE_SECRET=${DATABASE_SECRET}
ARG POSTGRES_HOST
ENV POSTGRES_HOST=${DATAPOSTGRES_HOSTBASE_SECRET}

ENTRYPOINT python query_db.py